import { CreationDateClientPipe } from './creation-date-client.pipe';

describe('CreationDateClientPipe', () => {
  it('create an instance', () => {
    const pipe = new CreationDateClientPipe();
    expect(pipe).toBeTruthy();
  });
});
